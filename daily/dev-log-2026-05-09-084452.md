# Dev Log — 2026-05-09 08:44:52 HKT

## Scope

Reviewed the local Symphony checkout, remote GitHub pull requests, local product
docs, and Linear tickets to summarize current implementation progress and choose
the next build items.

## GitHub PR Status

No open PRs are currently in review. `gh pr status --repo codatta/symphony`
reported no open PRs authored by the current user and no PRs requesting review.

Merged PRs inspected:

| PR | Merged | Scope |
|---|---|---|
| #1 | 2026-05-06 | Python multi-agent PRD and architecture |
| #2 | 2026-05-07 | MVP Linear API-key auth and tracker read path |
| #3 | 2026-05-08 | `WORKFLOW.md` typed config and hot reload |
| #4 | 2026-05-08 | `linear_graphql` client-side tool |
| #5 | 2026-05-08 | asyncio orchestration state machine |
| #6 | 2026-05-08 | agent runner base contracts |

## Local Checkout Status

Current branch: `feat/in-171-workspace-lifecycle`.

Uncommitted local work:

- `AGENTS.md` modified with Python environment rules.
- `prd.md` modified to mark workspace lifecycle progress and reference IN-171.
- `symphony/workspace.py` untracked.
- `tests/test_workspace.py` untracked.

Validation run:

```bash
uv run python -m unittest discover -s tests -p 'test_*.py'
```

Result: 38 tests passed.

## Product And Docs Status

The active product direction remains Phase 1: CLI-first MVP for Linear + Codex.
The MVP path is: repository-owned `WORKFLOW.md`, Linear polling, isolated
per-issue workspaces, bounded orchestration, Codex app-server execution,
`linear_graphql` handoff, and minimal status APIs.

`prd.md`, `SPEC.md`, and `ARCHITECTURE.md` are directionally aligned on the core
loop. `ARCHITECTURE.md` and later PRD phases describe broader post-MVP surfaces:
desktop app, web dashboard/PWA, OAuth/webhooks, notifications, approval gates,
and additional agent runners.

## Linear Status

Project: `Symphony — AI Agent Orchestration`.

Milestone progress from Linear:

- Phase 0: Guardrails — 13%.
- Phase 1: CLI MVP - Linear + Codex — 39%.
- Phase 2: Standalone App + Linear Productionization — 0%.
- Phase 3: Operator Visibility + Approval — 0%.
- Phase 4: Multi-Agent Runners — 0%.
- Phase 5: Desktop + IM Integrations — 0%.

Key ticket statuses:

- `IN-185` Linear read path: Done.
- `IN-170` WORKFLOW parser/hot reload: Done.
- `IN-168` `linear_graphql`: Done.
- `IN-169` orchestration state machine: Done.
- `IN-171` workspace lifecycle: In Progress locally.
- `IN-174` runner base classes: Linear says Todo, but PR #6 is merged remotely.
- `IN-175` Codex runner: Backlog.
- `IN-172` HTTP/SSE server: Todo.
- `IN-184` guardrails: In Review.

## Noted Mismatches

- Local `prd.md` marks workspace lifecycle complete, but `IN-171` is still In
  Progress in Linear and no PR is open for the local workspace lifecycle work.
- Linear `IN-174` still says Todo even though GitHub PR #6 for agent runner base
  contracts is merged.
- The active local branch is behind some merged remote feature branches; local
  code does not currently include all remote PR #5 and #6 files.

## Recommended Next 5 Build Items

1. Finish and PR `IN-171` workspace lifecycle.
2. Reconcile/import merged runner and orchestrator branches into the active
   integration branch.
3. Build `IN-175` Codex app-server runner.
4. Build `IN-172` minimal HTTP/status API.
5. Wire the full CLI MVP path end to end: CLI entrypoint, config preflight,
   Linear poll, workspace prepare, Codex run, issue handoff, retry/reconcile,
   and operator-visible logs.
