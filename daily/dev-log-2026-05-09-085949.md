# Dev Log — 2026-05-09 08:59:49 HKT

## Today's Goal

Complete Phase 1: CLI MVP — Linear + Codex.

For today, "complete Phase 1" means getting the repository to a state where the
CLI-first MVP loop is implemented, validated, and ready for review:

1. Symphony can load `WORKFLOW.md`, validate effective config, and start from a
   terminal entrypoint.
2. Symphony can poll Linear for active issues, normalize them, and maintain
   orchestration state.
3. Symphony can create or reuse a safe per-issue workspace, run lifecycle hooks,
   and clean up terminal workspaces.
4. Symphony can dispatch one issue to a Codex app-server runner and route
   `linear_graphql` tool calls through Symphony-managed Linear auth.
5. Symphony exposes enough status surface to debug the run: health, state,
   per-issue detail, refresh, and recent log access.

## Current Starting Point

No open GitHub PRs are in review for `codatta/symphony`.

Current branch: `feat/in-171-workspace-lifecycle`.

Local uncommitted work is already present for:

- `AGENTS.md` Python environment guidance.
- `prd.md` progress-review routine and workspace lifecycle notes.
- `daily/` dev logs.
- `symphony/workspace.py`.
- `tests/test_workspace.py`.

Linear Phase 1 status:

- Done: `IN-185`, `IN-170`, `IN-168`, `IN-169`.
- In progress locally: `IN-171`.
- Tracker mismatch: `IN-174` is still Todo in Linear, but GitHub PR #6 for
  runner base contracts is already merged.
- Remaining MVP blockers: `IN-175` Codex runner and `IN-172` minimal status API.

## Execution Order

### 1. Close Local Workspace Lifecycle Work

Target ticket: `IN-171`.

Tasks:

- Review `symphony/workspace.py` against `prd.md`, `SPEC.md`, and
  `ARCHITECTURE.md`.
- Run targeted workspace tests.
- Commit and open/update the PR for the workspace lifecycle slice.
- Update Linear with selected solution, validation, and PR link.

Done when:

- Workspace lifecycle tests pass.
- The PR exists and points to `IN-171`.
- Linear no longer disagrees with the local/PR state.

### 2. Reconcile Merged Remote Work Into The Active Branch

Target tickets: `IN-169`, `IN-174`.

Tasks:

- Bring merged orchestration and agent runner base work into the active Phase 1
  integration branch.
- Resolve any conflicts with workspace lifecycle changes.
- Update `prd.md` checkboxes only after the local branch actually contains the
  corresponding code.
- Update Linear `IN-174` to match merged PR #6 if it is still Todo.

Done when:

- Local branch contains orchestrator, runner contracts, workspace manager,
  workflow/config, Linear tracker, and `linear_graphql`.
- Full Python unit tests pass.

### 3. Implement Codex Runner

Target ticket: `IN-175`.

Tasks:

- Implement the concrete Codex app-server runner on top of the merged runner
  base classes.
- Launch Codex via configured command in the workspace directory.
- Normalize app-server events into Symphony events.
- Enforce turn timeout, read timeout, and stall timeout.
- Route `linear_graphql` tool calls to the Symphony Linear tool.
- Add unit tests with mocked stdio/process behavior.

Done when:

- Codex runner tests pass.
- Failure, timeout, malformed event, and tool-call paths are covered.

### 4. Implement Minimal Status API

Target ticket: `IN-172`.

Tasks:

- Add the MVP FastAPI status surface:
  - `GET /api/v1/health`
  - `GET /api/v1/state`
  - `GET /api/v1/<identifier>`
  - `POST /api/v1/refresh`
  - recent log access if not already available through the state payload
- Keep SSE and approval endpoints out of scope unless they are needed for the
  MVP contract.
- Add endpoint contract tests.

Done when:

- API tests pass.
- The API exposes enough information to trace issue id to workspace/run state.

### 5. Wire The End-To-End CLI MVP

Target tickets: `IN-158`, `IN-157`, `IN-175`, `IN-172`.

Tasks:

- Add or finish the CLI entrypoint.
- Run startup config preflight.
- Start workflow reload, Linear polling, orchestration, workspace prep, Codex
  runner dispatch, retry/reconcile, and status API together.
- Write a minimal operator usage flow in docs if any command or environment
  setup changed.

Done when:

- `symphony --help` works.
- A local dry-run or mocked end-to-end test proves the issue dispatch path.
- Relevant Python tests pass.
- The Phase 1 PR(s) include validation evidence and Linear links.

## Non-Goals For Today

- Linear OAuth, webhooks, setup wizard, or Keychain storage.
- Desktop app, Tauri sidecar, signed distribution, or app updates.
- Web dashboard/PWA, SSE dashboard stream, notifications, or approval gates.
- Claude, Gemini, OpenAI-compatible, or GPT-Image-1 runners.

## Validation Gate For Phase 1 Completion

Before calling Phase 1 complete:

```bash
uv run python -m unittest discover -s tests -p 'test_*.py'
```

Also run any targeted checks added for Codex runner subprocess behavior and HTTP
endpoint contracts. If a live Codex/Linear E2E cannot be run today, record the
reason and provide mocked proof plus the exact manual validation plan.

## Outcome Update

Parallel Codex workers completed the Phase 1 implementation lanes:

- `IN-171` workspace lifecycle is implemented locally in `symphony/workspace.py`
  with root containment, sanitized workspace keys, lifecycle hooks, hook
  timeouts, owner-only permissions, terminal cleanup, and `keep_on_failure`.
- `IN-169` orchestration state machine and `IN-174` agent runner contracts were
  integrated from their already-merged remote branches into the active branch.
- `IN-175` Codex runner is implemented in `symphony/agents/codex.py` with
  app-server JSON-RPC startup, turn execution, event normalization, timeout and
  malformed-frame handling, non-interactive approval/input handling, subprocess
  cleanup, and injectable `linear_graphql` routing.
- `IN-172` minimal status API is implemented in `symphony/http_server.py` as an
  offline-testable handler with `health`, `state`, per-issue detail, and refresh
  routes. FastAPI is not installed in the current environment, so the handler is
  framework-independent with a FastAPI factory for environments that install it.
- Runtime glue is implemented in `symphony/runtime.py`, coordinating one poll
  tick across tracker fetch, retry release/dispatch, workspace prep, prompt
  rendering, runner execution, event/state updates, and retry scheduling.
- CLI startup is wired in `symphony/cli.py` with `--check`, `--once`, runtime
  construction, status API construction, and a polling loop entrypoint.

Validation completed:

```bash
uv run python -m unittest discover -s tests -p 'test_*.py'
uv run symphony --help
git diff --check
```

Results:

- 100 Python tests passed.
- `symphony --help` works through the configured project script.
- `git diff --check` passed.

Remaining proof gap:

- No live Linear/Codex end-to-end run was executed in this environment. Current
  proof is offline and mocked for tracker, runtime, HTTP handler, and Codex
  app-server subprocess behavior. A follow-up live smoke test should run
  `symphony <WORKFLOW.md> --once` with real `LINEAR_API_KEY` and Codex auth.

## Milestone Decision

Stop Phase 1 testing here and close the milestone with caveat.

Additional local testing completed after implementation:

- `uv run symphony elixir/WORKFLOW.md --check --log-level WARNING` passed after
  `LINEAR_API_KEY` was exported.
- `uv run symphony elixir/WORKFLOW.md --once --log-level INFO` completed one
  live runtime tick with `fetched=0 dispatched=0 completed=0 failed=0
  released=0`.

Interpretation:

- Linear authentication and live polling work.
- No active candidate issues were present, so live workspace creation, Codex
  app-server dispatch, and live `linear_graphql` tool routing remain unproven.
- This remaining live-dispatch proof is now a Phase 2 entry gate.

Next milestone:

- Move focus to Phase 2: Standalone App And Linear Productionization.
- First Phase 2 gate: create or move exactly one disposable Linear issue into an
  active state and run `symphony elixir/WORKFLOW.md --once --log-level INFO` to
  prove live dispatch before building larger productionization surfaces.
